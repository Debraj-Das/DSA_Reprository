#include <stdio.h>
#include <stdlib.h>

void solve();

int main()
{ 
#ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
#endif

    int t = 1;
    scanf("%d",&t);
    while(t--) solve();
    
    return 0;
}

void solve()
{
    int n ;
    scanf("%d",&n);
    int arr[n];
    for (size_t i = 0; i < n; i++)
    {
        
    }
    
    return ;
}